# BatchGuard 阶段 1 测试

> 状态：实现验收通过（2026-07-29）。  
> 测试范围：最小工程骨架，不包含参数解析、扫描、哈希或重复分组。

> 历史说明：本文保留该阶段当时的构建目录、命令和结果；当前操作以根目录
> [README.md](../../README.md) 和 [交接文档.md](../交接文档.md) 为准。

## 1. 测试目标

验证阶段 1 满足以下条件：

- CMake 使用 C++20。
- `batchguard_core` 可以生成静态库。
- `batchguard_cli` 链接核心库并生成 `BatchGuard.exe`。
- Debug、Release 都能构建和运行。
- CLI 最终只输出一条启动信息并返回 `0`。
- 工程中不包含阶段 2 及后续业务实现。

## 2. 变更范围

```text
CMakeLists.txt
include/batchguard/core/batchguard_core.h
src/core/batchguard_core.cpp
apps/cli/main.cpp
```

目标关系：

```text
batchguard_cli ──> batchguard_core
```

## 3. 测试环境

| 项目 | 当前环境 |
|---|---|
| 操作系统 | Windows 10，版本 10.0.19043 |
| 编译器 | MSVC 19.29 |
| C++ 标准 | C++20 |
| CMake | 4.2 系列 |
| 构建工具 | Ninja |
| IDE | CLion |

构建目录属于本机生成内容，不进入 Git。

## 4. 测试用例

| 编号 | 场景 | 预期 | 方式 | 当前结果 |
|---|---|---|---|---|
| S1-01 | 配置 Debug | CMake 配置成功 | CMake/人工 | 通过 |
| S1-02 | 构建 Debug | 生成核心库和程序 | CMake/人工 | 通过 |
| S1-03 | 运行 Debug | 输出启动信息，返回 `0` | 人工 | 通过 |
| S1-04 | 配置并构建 Release | 生成 Release 程序 | CMake/人工 | 通过 |
| S1-05 | 检查目标关系 | CLI 链接核心库 | CMake 配置检查 | 通过 |
| S1-06 | 检查 C++ 标准 | 两个目标要求 C++20 | CMake 配置检查 | 通过 |
| S1-07 | 检查最终输出 | 只输出一条启动信息 | 源码与运行 | 通过 |
| S1-08 | 检查业务范围 | 没有扫描、哈希等实现 | 代码审查 | 通过 |
| S1-09 | GTest 测试骨架 | CTest 可发现烟雾测试 | GTest/CTest | 通过 |

S1-07 于 2026-07-29 重新验证：Debug、Release 均只输出
`BatchGuard started.`，输出行数为 `1`，退出码为 `0`。

S1-09 于 2026-07-29 完成：CMake 从 `Third_Party/googletest` 接入
GoogleTest 1.17.0，成功构建 `batchguard_tests.exe`，CTest 发现并通过
`Stage1SmokeTest.UsesCxx20StandardLibrary`。

## 5. 执行命令

CLion 中可选择 Debug/Release Profile 构建并运行 `batchguard_cli`。

终端 Debug：

```powershell
cmake -S . -B build/debug -G Ninja -DCMAKE_BUILD_TYPE=Debug
cmake --build build/debug
.\build\debug\BatchGuard.exe
```

终端 Release：

```powershell
cmake -S . -B build/release -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build/release
.\build\release\BatchGuard.exe
```

GTest/CTest：

```powershell
cmake --build build/debug --target batchguard_tests
ctest --test-dir build/debug --output-on-failure
```

## 6. 实际结果

2026-07-29 使用全新 `build/debug`、`build/release` 目录完成最终验收：

- Debug 成功生成 `batchguard_core.lib` 和 `BatchGuard.exe`。
- Release 成功生成 `batchguard_core.lib` 和 `BatchGuard.exe`。
- 两种配置运行退出码均为 `0`。
- 两种配置均只输出一行 `BatchGuard started.`。
- 修改 `main.cpp` 后，Ninja 只重新编译 CLI 源文件并重新链接程序。
- 新增核心源文件后，Ninja 重新生成核心静态库并重新链接程序。
- GoogleTest 使用本地 `Third_Party/googletest`，配置过程未重复下载源码。
- Debug 烟雾测试为 1/1 通过，总耗时 0.02 秒。
- Release 烟雾测试为 1/1 通过，总耗时 0.01 秒。

最终验收重跑时，Debug、Release CTest 均为 1/1 通过，总耗时均为 0.01 秒。

## 7. 已知问题

阶段 1 范围内没有阻塞问题。`.gitignore` 已排除 `build/`、`cmake-build-*`
和 `.idea/`；README 已包含 CLion、Debug、Release 和测试命令。

## 8. 阶段结论

阶段 1 实现验收通过：

- 核心库、CLI 和测试目标依赖关系成立。
- Debug、Release 构建和运行成功。
- CLI 保持单行启动输出且返回 `0`。
- GTest/CTest 烟雾测试在两种配置下通过。
- 构建产物和 IDE 配置已通过 `.gitignore` 排除。
- 没有提前实现阶段 2 业务。

本阶段没有业务逻辑，不要求业务单元测试；烟雾测试用于验证 C++20、GoogleTest、
CTest 和核心库测试目标的基本接线。代码与文档仍需由开发者在 CLion 中审查、
提交并推送。
