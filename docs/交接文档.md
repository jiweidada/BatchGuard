# BatchGuard 开发交接文档

> 最后更新：2026-07-29
>
> 项目路径：`E:\workspace\BatchGuard`
>
> 当前分支：`main`
>
> 远程仓库：`https://github.com/jiweidada/BatchGuard.git`

## 1. 使用说明

本文件用于在不同开发窗口之间传递当前状态。开始新会话时先阅读本文件和项目根
目录的 `AGENTS.md`，然后执行：

```powershell
git status --short --branch
```

实际 Git 状态优先于本文档。如果两者不一致，先查明原因并更新本文档，不要直接
开始下一阶段。

用户主要通过 CLion 手动选择文件、Commit 和 Push。未经用户明确要求，不要自动
提交或推送。

## 2. 长期开发规则

- 使用 C++20、CMake、Ninja 和 GoogleTest。
- 公共核心头文件放入 `include/batchguard/core/`。
- 核心实现放入 `src/core/`。
- CLI 代码放入 `apps/cli/`。
- 单元测试放入 `tests/unit/`。
- 每个阶段编写独立的 `docs/测试/阶段N测试.md`。
- 第三方源码统一放入 `Third_Party/<library>/`，不直接修改。
- 核心库不得依赖 CLI 或未来 GUI。
- `main.cpp` 只处理程序边界、对象组装和异常兜底。
- 项目自有代码注释必须使用中文。
- 注释内容和位置遵循 Google C++ Style Guide 的 Comments 章节。
- 第三方源码注释保持上游原貌，不翻译或修改。
- 文件系统预期错误使用 `std::error_code`。
- 文件系统测试使用运行时生成的临时目录和数据，不依赖固定盘符或个人路径。
- 不提交 `build/`、`cmake-build-*/` 和 `.idea/`。

## 3. 已完成并提交的阶段

### 阶段 1

已完成并推送：

- `batchguard_core` 静态库。
- `batchguard_cli` 和 `BatchGuard.exe`。
- GoogleTest/CTest 测试骨架。
- Debug、Release 构建。
- `docs/测试/阶段1测试.md`。

### 阶段 2

已完成并推送：

- 单目录参数。
- `--help`。
- `--version`。
- 输入路径存在性和目录类型验证。
- Windows 宽字符参数。
- UTF-8 控制台输出。
- 无效输入退出码 `1`。
- `docs/测试/阶段2测试.md`。

阶段 2 的主要提交：

```text
58eae49 feat: 添加输入目录校验功能
2d8c3fe feat: 添加输入目录验证接口
9c0f72a feat: 实现阶段2 CLI参数与目录输入验证
```

### 阶段 3

已完成并推送：

- 递归发现普通文件。
- 空目录返回零文件。
- 支持嵌套目录。
- 不按扩展名过滤。
- 不跟随符号链接和 Windows junction。
- 使用 `std::error_code` 处理预期文件系统失败。
- 使用 `FileFailure` 保存发现失败。
- 文件路径和失败记录稳定排序。
- 支持中文及空格路径。
- CLI 显示普通文件数和发现失败数。
- 完整发现返回 `0`，局部发现失败返回 `2`。
- 不读取文件内容、文件大小，不计算哈希或判断重复文件。

阶段 3 提交：

```text
1d8747a feat: 完成阶段3递归文件发现
```

### 阶段 4

已完成、提交并 Push：

- 读取文件大小并按大小筛选候选。
- 使用 Windows CNG `BCrypt` 计算 SHA-256。
- 使用 64 KiB 固定缓冲区分块读取。
- 元数据和哈希失败隔离。
- CLI 显示阶段四处理计数。
- `docs/测试/阶段4测试.md`。

阶段 4 提交：

```text
30d4d1f feat: 完成阶段4内容指纹
```

### 阶段 5

已完成、提交并 Push：

- 根据“文件大小 + SHA-256”建立跨目录重复组。
- 建立完整 `ScanReport`。
- 输出中文汇总、重复组和失败详情。
- 完成 MVP M-01 至 M-11 行为实现。
- `docs/测试/阶段5测试.md`。

阶段 5 提交：

```text
d9988a2 feat: 完成阶段5重复分组与扫描报告
```

### 阶段 6

已完成、提交并 Push：

- 建立 MVP M-01 至 M-11 覆盖矩阵。
- 补充批量候选、重复扫描、只读性和文件变化测试。
- 完成 53 项 Debug、Release 全量回归。
- `docs/测试/阶段6测试.md`。

阶段 6 提交：

```text
138486e chore: 完成阶段6测试与健壮性回归
```

### 阶段 7 和阶段 8

已完成、合并提交并 Push：

- 阶段 7 扫描进度事件和控制台动态显示。
- 阶段 8 有界文件级并发和 `--hash-workers 1-64`。
- Debug、Release 73 项全量回归。
- `docs/测试/阶段7测试.md`。
- `docs/测试/阶段8测试.md`。

阶段 7 和阶段 8 合并提交：

```text
180eede feat: 支持并发哈希与控制台进度显示
```

### 阶段 9

已完成并提交，尚未 Push：

- 完成 MVP 最终交付整理和验证。
- 新增版本变更记录、演示问答和最终交付清单。
- 将文档分类为规划、设计、学习、测试和交付。
- 保留 `docs/交接文档.md` 和 `docs/文档说明.md` 作为操作入口。
- 更新 README、AGENTS 和全部相对链接。

阶段 9 提交：

```text
docs: 整理 v0.1.0 最终交付文档
```

## 4. 当前阶段：v0.1.0 交付完成

阶段 1 至阶段 9 均已实现、验证并提交。当前本地 `main` 比 `origin/main` 领先
阶段 9 的 1 个文档提交，工作区干净；该提交尚未 Push。

真实存储目录的 1、2、4、8 线程性能比较属于后续调优项，不记录业务目录或文件名。

### 新对话快速接手

新对话开始后按以下顺序继续：

1. 阅读项目根目录的 `AGENTS.md` 和本交接文档。
2. 执行 `git status --short --branch`，实际状态优先于本文档。
3. 最新本地提交是 `docs: 整理 v0.1.0 最终交付文档`。
4. 阶段 1 至阶段 9 均已实现、验证并提交；阶段 9 提交尚未 Push。
5. 如需性能调优，在真实 CMD 或 PowerShell 中按同一目录比较并发度：

```cmd
.\build\release\BatchGuard.exe --hash-workers 1 "<测试目录>"
.\build\release\BatchGuard.exe --hash-workers 2 "<测试目录>"
.\build\release\BatchGuard.exe --hash-workers 4 "<测试目录>"
.\build\release\BatchGuard.exe --hash-workers 8 "<测试目录>"
```

6. 未经用户明确要求不要 Commit 或 Push。

## 5. 阶段 3 验证结果

- Debug 构建成功，没有新增编译警告。
- Release 构建成功，没有新增编译警告。
- CTest 发现 24 个测试，0 个失败。
- 当前 Windows 实际执行 23 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.12 秒。
- Release CTest 总耗时约 0.09 秒。
- Debug、Release 真实 CLI 均在嵌套中文目录中发现 2 个普通文件并返回 `0`。
- Windows junction 手工验证通过，发现器未进入联接目标。
- 手工测试数据已清理。

阶段 3 CLI 输出示例：

```text
目录验证通过：<目录>
发现普通文件：2
发现失败：0
阶段 3 仅发现文件，尚未计算哈希或重复分组。
```

## 6. 阶段 3 测试命令

运行全部 Debug GTest：

```powershell
.\build\debug\tests\batchguard_tests.exe --gtest_color=no
```

只运行阶段 3 文件发现测试：

```powershell
.\build\debug\tests\batchguard_tests.exe `
    --gtest_filter=FileDiscoveryTest.* `
    --gtest_color=no
```

运行 CTest：

```powershell
ctest --test-dir build/debug --output-on-failure
ctest --test-dir build/release --output-on-failure
```

## 7. 当前待办

1. 用户决定是否 Push 阶段 9 文档提交。
2. 用户决定是否创建并 Push `v0.1.0` Git tag 或发布包。
3. 后续按需在真实存储目录上比较 1、2、4、8 个哈希线程的总耗时。

## 8. 阶段 4 边界

阶段 4 计划范围：

- 读取文件大小。
- 按文件大小建立候选组。
- 使用 SHA-256 计算内容指纹。
- 分块读取文件，不一次性加载完整文件。
- 单个文件读取失败时记录失败并继续。
- 添加阶段 4 GTest。
- 编写 `docs/测试/阶段4测试.md`。

阶段 4 应作为新的独立提交，不修改已经完成的阶段 3 提交。

阶段 4 已采用以下技术方案：

- Windows CNG `BCrypt` SHA-256，不新增第三方依赖。
- 64 KiB 固定缓冲区分块读取。
- 先读取文件大小，只哈希至少有两个成员的同大小候选组。
- 元数据失败记录为 `Metadata`，文件打开或读取失败记录为 `Hashing`。
- 单文件失败不终止其他文件处理。
- 本阶段不建立重复组或完整 `ScanReport`。

## 9. 阶段 4 验证结果

- Debug 构建成功，没有新增编译警告。
- Release 构建成功，没有新增编译警告。
- Debug 和 Release CTest 均发现 36 个测试，0 个失败。
- 当前 Windows 实际执行 35 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.20 秒。
- Release CTest 总耗时约 0.14 秒。
- 已知输入、零字节文件和跨多个读取块的大文件 SHA-256 均验证通过。
- 独占锁定文件形成 `Hashing` 失败后，其他同大小候选仍继续处理。
- Debug、Release 真实 CLI 均发现 3 个普通文件、计算 2 个内容指纹、记录 0 个
  失败并返回 `0`。
- 手工测试数据已清理。

## 10. 阶段 5 边界

阶段 5 已完成以下范围：

- 根据“文件大小 + SHA-256”建立重复文件组。
- 只保留至少两个成员的重复组。
- 对组内路径和重复组进行稳定排序。
- 建立完整 `ScanReport`。
- 输出中文重复组、汇总信息和失败记录。
- 添加阶段 5 GTest。
- 编写 `docs/测试/阶段5测试.md`。

阶段 5 修改没有混入已经提交的阶段 4。

## 11. 阶段 5 验证结果

- Debug 构建成功，没有新增编译警告。
- Release 构建成功，没有新增编译警告。
- Debug 和 Release CTest 均发现 49 个测试，0 个失败。
- 当前 Windows 实际执行 48 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.28 秒。
- Release CTest 总耗时约 0.19 秒。
- 跨子目录、同名不同内容、同大小不同内容、零字节和三个重复文件均验证通过。
- 独占锁定文件形成部分报告后，其他文件继续处理并返回 `2`。
- Debug、Release 真实 CLI 均发现 5 个文件、成功处理 5 个、输出 1 个跨中文
  子目录重复组、记录 0 个失败并返回 `0`。
- 手工测试数据已清理。

## 12. 阶段 6 边界

阶段 6 已完成以下范围：

- 复核 MVP M-01 至 M-11 的自动化覆盖。
- 补充仍缺少的健壮性和 CLI 集成测试。
- 检查大目录、文件变化和错误展示等边界。
- 完成 Debug、Release 全量回归。
- 编写 `docs/测试/阶段6测试.md`。

阶段 6 没有修改核心业务实现，也没有混入阶段 7 交付内容。

## 13. 阶段 6 验证结果

- Debug 构建成功，没有新增编译警告。
- Release 构建成功，没有新增编译警告。
- Debug 和 Release CTest 均发现 53 个测试，0 个失败。
- 当前 Windows 实际执行 52 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.36 秒。
- Release CTest 总耗时约 0.25 秒。
- 120 个同大小候选文件成功形成 60 个重复组。
- 重复扫描结果稳定，输入内容和最后写入时间保持不变。
- 文件在发现后消失时记录 `Metadata` 失败并继续处理。
- 锁定文件报告包含非空错误原因并返回 `2`。
- Debug、Release 真实 CLI 均发现 5 个文件、输出 1 个跨中文子目录重复组并
  返回 `0`。
- 手工测试数据已清理。

## 14. 阶段 7 边界

阶段 7 已调整为以下范围：

- 文件发现阶段显示累计发现数量。
- 元数据读取阶段显示文件完成比例。
- SHA-256 阶段显示候选文件和读取字节进度。
- 重复分组阶段显示生成状态。
- 只在真实控制台中动态刷新。
- 保持核心库与 CLI 展示解耦。
- 添加阶段 7 自动化测试和测试文档。

原阶段 7 的 MVP 最终交付整理顺延为阶段 8。

## 15. 阶段 7 验证结果

- Debug 构建成功，没有新增编译错误。
- Release 构建成功，没有新增编译错误。
- 阶段 7 的 6 项重点测试全部通过。
- Debug 和 Release CTest 均发现 59 个测试，0 个失败。
- 当前 Windows 实际执行 58 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.40 秒。
- Release CTest 总耗时约 0.27 秒。
- 1,000,000 字节文件的哈希进度按 64 KiB 读取块单调递增。
- 文件读取失败后仍完成哈希和重复分组进度，并生成部分报告。
- 控制台渲染器的启用和禁用行为均通过自动化测试。
- 使用用户的 7 文件目录运行 Debug CLI，正确输出 1 个跨目录重复组并返回 `0`。
- 工具捕获形成非控制台输出，实际 CLI 只输出最终报告，没有动态进度文本。
- 用户在真实 Windows 控制台的大目录扫描中确认动态进度能够显示。

## 16. 阶段 8 边界

阶段 8 已完成以下范围：

- 候选文件之间使用有界工作线程并发计算 SHA-256。
- 单个文件继续使用 64 KiB 固定缓冲区顺序读取。
- 默认自动使用最多 4 个工作线程。
- 支持 `--hash-workers 1-64` 手动设置并发度。
- 使用 `std::jthread` 和 `std::stop_token` 管理线程生命周期。
- 工作线程不直接调用进度回调，由扫描线程汇聚并串行报告进度。
- 单文件失败继续隔离，最终结果继续稳定排序。
- 增加阶段 8 调度器、真实文件并发和 CLI 参数测试。
- 编写 `docs/测试/阶段8测试.md`。

## 17. 阶段 8 验证结果

- Debug 构建成功，没有新增编译警告。
- Release 构建成功，没有新增编译警告。
- Debug 和 Release CTest 均发现 73 个测试，0 个失败。
- 当前 Windows 实际执行 72 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.53 秒。
- Release CTest 总耗时约 0.35 秒。
- 7 项并发核心测试在 Release 下随机顺序重复 50 轮，共 350 次执行全部通过。
- 可控测试确认至少两个不同工作线程同时执行哈希任务。
- 真实文件的 1 线程和 4 线程结果完全一致。
- 多线程进度保持单调，回调始终在发起扫描的线程执行。
- 文件哈希失败后其他工作线程继续完成。
- Debug CLI 使用 `--hash-workers 4` 扫描项目文档成功并返回 `0`。

## 18. 阶段 9 边界

阶段 9 已完成 MVP 最终交付整理：

- 复核版本号、帮助文本和 README 使用说明。
- 汇总 Debug、Release 构建和测试结果。
- 复核退出码、错误展示和输出重定向行为。
- 清理交接文档中的历史待办，形成最终交付清单。
- 不新增重复文件删除、移动或修改功能。

## 19. 阶段 9 验证结果

- 版本保持 `0.1.0`，帮助文本和 README 使用方式一致。
- `--help`、`--version` 返回 `0`，缺参和未知选项返回 `1`。
- 锁定文件自动化场景返回 `2`，其他文件继续处理。
- 1 线程和 4 线程扫描相同目录的最终报告完全一致。
- 非控制台输出不包含动态进度文本。
- 重复扫描只读性测试通过。
- Debug、Release 配置和构建成功，没有新增编译警告。
- Debug 和 Release CTest 均发现 73 个测试，0 个失败。
- 当前 Windows 实际执行 72 个测试并通过。
- 目录符号链接测试因当前机器没有创建符号链接权限而标记为 Skipped。
- Debug CTest 总耗时约 0.59 秒。
- Release CTest 总耗时约 0.41 秒。
- 新增 `CHANGELOG.md`、演示问答、阶段 9 测试记录和最终交付清单。
- 文档已分类到 `docs/规划/`、`docs/设计/`、`docs/学习/`、`docs/测试/` 和
  `docs/交付/`。
- README、AGENTS 和 Markdown 相对链接已同步更新。

## 20. 构建环境说明

CLion 构建环境可以正常找到 MSVC 标准库。普通 PowerShell 如果没有加载 Visual
Studio 开发环境，`cl.exe` 可能找不到 `<filesystem>` 等标准库头文件。终端构建
应使用 Visual Studio Developer PowerShell、Developer Command Prompt，或直接
通过 CLion 构建。
