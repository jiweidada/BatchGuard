# BatchGuard 变更记录

本文记录 BatchGuard 各版本面向使用者和开发者的重要变化。

## 0.2.0 - 2026-07-31

- 新增可选 Qt Widgets GUI 和独立 `BatchGuardGui.exe`。
- 支持目录选择、哈希线程设置、后台扫描、四阶段进度和协作式安全取消。
- 扫描中关闭窗口时先安全停止并回收工作线程。
- 展示自然语言结论、六项指标、重复组主表、完整路径和失败详情。
- 取消请求在完成事件竞争中保持取消终态，重复组排序后保留当前路径联动。
- 理论可节省空间使用只读逻辑大小估算，不提供删除或自动清理。
- 新增结构化运行日志：GUI 显示简化状态，CLI 和父终端显示完整字段。
- 完成 100%、125%、150% DPI、真实目录、取消、关闭和最小窗口布局验收。
- 提供 Qt 5.14.2 Windows x64 Release 部署说明和隔离环境启动验证。
- 保留原有 CLI，并允许关闭 GUI 选项后在没有 Qt 的环境中构建。

## 0.1.0 - 2026-07-29

首个可交付版本：

- 支持 Windows 宽字符命令行参数、`--help` 和 `--version`。
- 验证单个输入目录，并使用明确退出码区分输入错误、局部失败和内部错误。
- 递归发现普通文件，不跟随目录符号链接或 Windows junction。
- 先按文件大小筛选候选，再使用 Windows CNG 分块计算 SHA-256。
- 按“文件大小 + SHA-256”建立稳定排序的重复文件组。
- 隔离发现、元数据和哈希失败，单文件失败不终止整个扫描。
- 在真实控制台中显示发现、元数据、哈希和分组进度。
- 使用有界工作线程并发处理候选文件，支持 `--hash-workers 1-64`。
- 提供 Debug、Release 构建说明及 GoogleTest/CTest 自动化测试。

该版本保持只读，不提供删除、移动、重命名或修改文件功能。
